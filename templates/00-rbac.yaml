apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lernvirt.fullname" $ }}-gateway
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "lernvirt.fullname" $ }}-secret-manager
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "lernvirt.fullname" $ }}-secret-manager
subjects:
  - kind: ServiceAccount
    name: {{ include "lernvirt.fullname" $ }}-gateway
roleRef:
  kind: Role
  name: {{ include "lernvirt.fullname" $ }}-secret-manager
  apiGroup: rbac.authorization.k8s.io
